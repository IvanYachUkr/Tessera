[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "terrapulse_features"
version = "0.1.0"
requires-python = ">=3.8"

[tool.maturin]
module-name = "terrapulse_features"
features = ["pyo3/extension-module"]
