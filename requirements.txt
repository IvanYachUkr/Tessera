# ============================================================
# TerraPulse — Python Dependencies
# ============================================================
#
# Setup (Windows):
#   python -m venv .venv
#   .venv\Scripts\activate
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt
#
# This installs CUDA-enabled PyTorch first, then everything else.
# JAX is CPU-only (GPU JAX on Windows requires Docker/WSL).
# ============================================================

# ---- Core geospatial ----
rasterio
geopandas
shapely
pyproj
rasterstats

# ---- Satellite data access ----
pystac-client
planetary-computer
stackstac
rioxarray
xarray

# ---- Data processing ----
numpy
pandas
pyarrow
scipy
tqdm
pyyaml

# ---- ML: classical ----
scikit-learn
catboost
optuna
shap

# ---- ML: deep learning ----
# NOTE: torch is installed separately with CUDA (see header)
jax
jaxlib

# ---- Image processing / features ----
scikit-image

# ---- Spatial statistics (Moran's I) ----
libpysal
esda

# ---- Visualization ----
matplotlib
seaborn
folium

# ---- Dashboard ----
streamlit
streamlit-folium

# ---- OpenStreetMap ----
osmnx
